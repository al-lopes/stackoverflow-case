models:
  - name: ranking_tags
    description: Tags ranked by number of questions and total views, and number of questions without answer per tag
    columns:
      - name: tag_id
        description: The unique key for each tag.
        data_tests:
          - not_null
          - unique
          - relationships:
              arguments:
                to: ref('stg_tags')
                field: tag_id