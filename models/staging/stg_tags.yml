models:
  - name: stg_tags    
    description: dim_tags staging table from tags table in Stackoverflow public dataset
    columns:
      - name: tag_id
        description: The unique key for each tag.
        data_tests:
          - not_null
          - unique
